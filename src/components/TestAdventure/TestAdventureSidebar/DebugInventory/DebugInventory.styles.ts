import styled from "styled-components";

export const DebugInventoryTitle = styled.h2`
  margin: 0 0 var(--space-3) 0;
  font-size: var(--font-size-lg);
`;

export const DebugInventoryList = styled.ul`
  list-style: none;
  padding: 0;
  margin: 0;
`;

export const DebugInventoryItem = styled.li`
  margin-bottom: var(--space-2);
`;
